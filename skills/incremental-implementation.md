# incremental-implementation

Use for one small implementation slice. Keep the change reviewable, obey the declared scope, run the acceptance command after implementation, and stop rather than expanding into a second feature.

Evidence: changed files match scope, acceptance passes, task report names what moved.
