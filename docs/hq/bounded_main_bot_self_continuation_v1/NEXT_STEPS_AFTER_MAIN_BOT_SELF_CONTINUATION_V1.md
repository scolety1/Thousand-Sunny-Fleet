# Next Steps After Main Bot Self-Continuation V1

## Ready For Local Manual Use

- Bounded Project Main Bot self-continuation for fixture-only TSF infrastructure missions.
- Role-aware dry-run routing with explicit approval ledger checks.
- One foreground Codex worker handoff through `service_tier=fast` and `workspace-write`.
- Verifier, context capsule, loop-prevention, and preservation evidence after worker output.

## Recommended Next Milestone

`Local Mission Queue Foreground Executor V1`

This should connect the already-merged local mission queue/inbox/outbox foundations to the proven bounded foreground worker path. It should remain local, foreground, fixture-only at first, and should not add background workers or broad product repo authority.

## Should Wait

- Operator Console implementation should wait until queue execution behavior is proven locally.
- API/HQ transport should wait until compressed escalation packet boundaries are stable.
- True parallel Codex workers should wait until isolated worktree pilot approval.

## Do Not Build Yet

- Background worker pool.
- Persistent scheduler/watchdog.
- Product repo mission execution.
- Canonical NWR mutation.
- Push/merge/deploy automation.
- Source-truth, ranking, formula, app wiring, recommendation, or hidden sort promotion.
