# TSF HQ Dispatch Request-to-Result V1A

Verdict: `GREEN_TSF_HQ_DISPATCH_REQUEST_RESULT_V1A_READY_FOR_INDEPENDENT_AUDIT`.

Adoption classification: `ADOPT_EXISTING_WORKTREE_AS_AUTHORITATIVE_M2_CANDIDATE`.

The exact linked worktree was moved with `git worktree move` from `C:\Users\codex-agent\Documents\Vacation\Thousand-Sunny-Fleet-hq-dispatch-request-result-relay-v1-20260714` to `C:\TSF_M2A`. The shared Git directory, branch, starting HEAD, index, 4 tracked modifications, and 33 untracked Milestone 2 files were preserved. Pre/post tracked patch SHA-256 is `b5be23f23f96e61de62a92e9d68d2fb15d522f419cc6588aa1ffea8547b48f22`; pre/post untracked archive SHA-256 is `f95f322b0a0a6e6b7cd4fd6644b5f95ee3c087fe6d12ef6a91b1d2c918d8f6a2`. All 37 candidate paths, bytes, modes, and Git-state identities matched after relocation.

The canonical runtime maximum fell from `234` to `139`, below the unchanged accepted maximum `225`. The measurement used the production `New-TsfCompleteRuntimePathPlan` resolution at the real filesystem path; no alias, junction, SUBST drive, or lexical-only path was used.

All blocked canonical suites passed. The HQ Dispatch HTTP/security/replay suite passed 79 assertions. The deterministic canonical slice passed 36 assertions and reached `ADMITTED`. Exactly one real read-only app-server mission reached `ADMITTED_WITH_CAVEATS`; the caveats are limited to the app-server not exposing effective effort and its thread default differing from the explicit turn request. The explicit turn request was acknowledged, the worker returned the required terminal result, admission succeeded, and the owned child exited without an orphan or listener.

The display/preservation-only `TIM_REQUIRED` behavior remains intentionally bounded: it preserves the exact request and evidence, proves the original run terminal, and infers no answer. Approval, denial, and clarification response writing remain Milestone 2B.

Exact next action: one independent read-only audit of the completed Milestone 2A commit.
