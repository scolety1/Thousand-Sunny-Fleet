# Deep Research Synthesis Engine V1

Historical verdict (superseded): GREEN_SYNTHESIS_ENGINE_LOCAL_FIXTURE_READY

Current status: CONTENT_DERIVED_DETERMINISTIC_MARKDOWN_V1 / SCRIPT_BACKED_NOT_UI_WIRED / ADVISORY_ONLY.

The corrected script reads the preserved bodies of unique `IMPORTED_VALID` reports for the plan's exact project and prompt IDs. It requires non-empty Summary, Findings, Recommendations, Caveats, and Sources sections; extracts explicit recommendation signals; attributes claims and decisions to prompt ID, report hash, and source entries; and records content-derived agreements and unresolved disagreements. Missing or ineligible evidence produces YELLOW. It never treats research as approval and never calls an API, LLM, worker, or external service.

All reports used by this packet are `SYNTHETIC_FIXTURE_NOT_REAL_RESEARCH`. The bounded correction packet is the authoritative PR #11 validation record.
