# Context Capsule Update Writer V1

`tools/Update-TsfProjectContextCapsule.ps1` updates a local project context capsule with mission id, result, worker role, lane, artifacts, blockers, approvals, next action, and do-not-repeat lessons. It is a foreground file writer only, not a persistent memory service.
