# Real general-result proofs

This file is populated only from exact-candidate, detached proof evidence. Required proof classes are:

1. Successful deterministic TSF-local general analysis with required deliverable, independent verifier GREEN, preservation, admission, and receipt.
2. Controlled worker inability with successful transport, semantic failure, verifier rejection, and no admission receipt.
3. Detached write/commit request with bound original intent and `TIM_REQUIRED_NO_QUEUE` before worker allocation.
4. Unique exact-literal admission plus exact mismatch rejection.
5. Scope-reduction fixture demonstrating that the original preview cannot authorize a different read-only mission.

Synthetic focused gates are prerequisites, not substitutes for this exact-candidate proof record.
