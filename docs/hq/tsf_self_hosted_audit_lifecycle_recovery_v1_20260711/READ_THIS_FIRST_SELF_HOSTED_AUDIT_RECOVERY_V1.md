# TSF self-hosted audit lifecycle recovery V1

This packet records the bounded repair for the stopped self-hosted foundation audit. It does not restart the overnight program and contains no plugin work.

The preserved attempt `tsf-foundation-authority-audit-bd35f991-20260711` remains failed history. Its stop record and queue snapshot were not changed. A retry must use the repository-relative canonical preparation entry point, a new mission identity, the exact repaired HEAD, and one foreground stable app-server child with worker-tool network disabled.

Local result: the lifecycle now emits exactly one schema-validated terminal `lc.json` result for success, caveat, preflight, role, worker-start, worker-result, verifier, preservation, TIM-required, and internal-error outcomes. The result is derived from the shared complete runtime path plan and registered through the run-scoped producer capability before the executor consumes it.

Publication remains conditional. Only an admitted independent Auditor verdict of `GREEN_TSF_FINAL_AUTHORITY_BOUNDARIES_ACCEPTED` or `GREEN_TSF_FINAL_AUTHORITY_BOUNDARIES_ACCEPTED_WITH_NONBLOCKING_CAVEATS` permits pushing the exact audited commit and opening the PR. No merge is authorized.
