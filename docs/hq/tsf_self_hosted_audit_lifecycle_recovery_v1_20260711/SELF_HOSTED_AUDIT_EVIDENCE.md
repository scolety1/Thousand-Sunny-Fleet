# Self-hosted Auditor evidence

The committed repair intentionally contains the audit protocol, not a fabricated post-commit result. The one authorized Auditor mission is prepared only after the repair commit so its durable mission, policy fingerprint, queue document, lifecycle result, preservation packet, durable result, admission receipts, and queue transition can bind the exact committed HEAD.

Required mission properties:

- new mission ID; never reuse `tsf-foundation-authority-audit-bd35f991-20260711`;
- role `auditor_worker`, model alias `DEEP`, resolved model `gpt-5.6-sol`, effort `HIGH`;
- read-only sandbox, control-plane network `CODEX_SERVICE_ONLY`, worker-tool network `DISABLED`;
- one stable non-experimental foreground app-server child;
- source scope limited to the final three authority corrections and this lifecycle repair;
- no file mutation, nested worker, rerun, browsing, package install, Git network, or external API;
- exact accepted verdicts only: `GREEN_TSF_FINAL_AUTHORITY_BOUNDARIES_ACCEPTED` or `GREEN_TSF_FINAL_AUTHORITY_BOUNDARIES_ACCEPTED_WITH_NONBLOCKING_CAVEATS`.

After execution, the authoritative evidence is the ignored compact runtime packet bound to the audit mission and the queue executor result. Publication must stop unless verifier, preservation, durable mapping, admission, receipts, final queue state, and child cleanup are all GREEN and bound to the repair commit.
