# TSF V1 response-contract and runtime-cleanliness hotfix

This packet covers the combined V1 hotfix for exact-response propagation, mission/revision identity, generated-runtime cleanliness, exact interruption ownership and shutdown, and responsive operator UI blockers reproduced by the corrected canonical first-launch closeout.

- Required base: `7cceadcf6fa6a6c65000c72604023f87fc84f728`
- Required base tree: `b6abd94055b60f77666b2f1ba807c6016537c1dd`
- Branch: `hotfix/tsf-response-contract-runtime-cleanliness-v1-20260718`
- Worktree: `C:\TSF_HOTFIX2`
- Exact requested literal: `TSF_V1_CANONICAL_FIRST_LAUNCH_GREEN`
- Exact SHA-256: `192168669db5ba0e1e6eb6877f2ce775defd0654a0fe4e124621aa7b9607c627`

The implementation adds `EXACT_LITERAL_V1`, removes the M2A fixture literal as an unrelated mission default, validates the same semantic contract at preview, submission, mission, worker, verifier, admission, and UI boundaries, and narrowly ignores only generated mission-revision JSON records in the existing canonical queue states. Doctor now invokes the complete canonical queue-document validator for every trusted generated record and rejects malformed, incomplete, wrong-state, duplicated, unknown, unreadable, symbolic-link, junction, mount-point, and other reparse-backed entries before reading through them. Tracked, staged, and unrelated untracked source changes remain unsafe.

The real interruption gate now binds readiness to the authoritative app-server spawn and exact proof-owned PID/start-time/parent/executable/capability/candidate/run identities. Its recovery assertion derives the applicable result contract from the newly prepared recovery revision: `EXACT_LITERAL_V1` still requires complete exact evidence, while a mission with no exact-response contract requires canonical null/absence and is classified in proof output as `NOT_APPLICABLE_NO_EXACT_RESPONSE_CONTRACT`. The non-authoritative V1 raw worker payload may omit root `mission_revision`; the canonical queue, adapter, producer registry, lifecycle wrapper, verifier, preservation manifest, durable result, and admission records must still bind the exact revision and run. Production evidence is never fabricated to satisfy the harness.

Exact Stop now authenticates with a stable hash derived only from the immutable server/process/listener/session/capability identity. The mutable whole-owner evidence hash remains a registry-integrity value, but registry or ledger evolution cannot invalidate an otherwise exact Stop request. The proof still refreshes the live owner immediately before Stop and fails closed unless server instance, PID/start time, listener, mission/revision/run/result, token hash, and required proof-owned process identities remain exact. The immutable owned-process registry remains ownership authority; the append-only action ledger remains its causal audit projection. No unattributed process may be targeted.

Authoritative app-server spawn inspection now uses `Get-Process` for live process identity and start time, with CIM used for parent identity and executable corroboration. Nullable CIM fields cannot be misclassified as an ownership mismatch; every failure carries its exact invariant and observed identity. A controlled same-binary app-server probe and the cycle-6 real proof confirmed exact PID/start/executable/parent binding.

The responsive correction removes the fixed page minimum and lets grid/flex children shrink, long authority identifiers wrap, genuine preformatted evidence scroll within a bounded container, and narrow action groups wrap. It does not use page-level overflow hiding and does not hide authority content.

The corrected pre-amend gates are truthfully labeled in `EXECUTED_TEST_COVERAGE.csv`; they do not claim final-commit binding. The managed-wrapper failure that exposed the invalid universal exact-evidence assumption is preserved with `EXIT_NOT_RELIABLY_OBSERVED`, not a fabricated numeric exit. The committed final-acceptance, real production, wrong-result, interruption, responsive-browser, and packet-self-check harnesses are the self-bound post-amend gates. Their ignored evidence must record the exact final HEAD/tree, commands, exits, counts, classifications, output hashes, and cleanup state from a fresh detached deep-proof worktree. Publication remains prohibited until those gates and both independent reviews are GREEN.

The full-acceptance preflight now uses the canonical kernel Git-state classification through a bounded diagnostic adapter. A detached checkout is recorded as `DETACHED_COMMIT_PINNED`, with a null branch, exact HEAD/tree, and `DETACHED_READ_ONLY_ACCEPTANCE_PERMITTED_WRITES_REMAIN_DENIED`; Git command failures remain `UNKNOWN_OR_INVALID_GIT_STATE` and fail closed with command, exit, repository, and stderr hash evidence. Nested acceptance fixtures that operate on the current repository use the same canonical state instead of dereferencing empty `git branch --show-current` output. Read-only mission fixtures set `branch_required=false` and `expected_branch=null` only when the canonical state is detached; attached behavior and detached write denial are unchanged.

The corrected working-tree acceptance ran from the short, clean, detached disposable worktree `C:\TSFDA4` to avoid conflating Windows path-budget policy with the harness correction. It passed 31/31 checks and 1,159 criteria with exit 0, executed the 186-assertion real interruption/recovery proof, retained a null branch, and finished with safe Doctor disposition, no owner, no listener, no proof-owned child, and clean tracked state. Its disposable test identity (`0f53ec6f945a1e03c4a9962296548e311c4d2496`, tree `4ba7a5794f32dbfaea4011d03d2b822b01d3da1f`) is explicitly pre-amend evidence, not the final publication candidate.

The overnight native recovery isolated a nondeterministic Node-on-Windows abort/Stop shutdown race. The test now settles the aborted response, captures and closes the exact server outside the Stop handler, awaits close, and serializes durable stage/process evidence. Packet-seal Proof 2 recorded two native child exits 0, two distinct real interruption/recovery proofs, and corrected-root full acceptance at 31/31 checks and 1,163 criteria. See `NATIVE_FAST_FAIL_ROOT_CAUSE.md`, `NATIVE_FAST_FAIL_DIAGNOSTICS.md`, and `FETCH_TRACE_AND_DURABLE_PROJECTION.md`. Tracked Proof 2 rows are explicitly `PRE_FINAL_PACKET_SEAL_PROOF2`, never mislabeled as the successor publication commit. The packet binds the publication candidate as the single commit containing this packet; fresh ignored detached proof inputs provide the runtime HEAD/tree check without attempting an impossible self-referential commit hash.

No historical closeout bytes are rewritten. No merge, deployment, product-repository access, plugin, credential access, package installation, service, task, startup registration, remote listener, or persistent process is authorized by this packet.
