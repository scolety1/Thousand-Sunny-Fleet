# Workflows

## Report Export

The sample app has a report export workflow for TSF repo onboarding detector
tests. The workflow exists in docs, a package script, a PowerShell script, app
code, and a test file.
